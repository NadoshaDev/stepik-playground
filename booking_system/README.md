#  Мини-проект: Система записи клиента

> Учебный проект по разработке системы записи клиента на свободное время (запись к мастеру).

> Проект развивается от версии к версии и служит практической площадкой для изучения Python и принципов разработки программного обеспечения.

> Главная цель — не просто написать код, а понять логику работы программы, научиться обрабатывать ошибки и принимать инженерные решения.


---

##  Общая логика проекта

Есть:
- Список всех доступных слотов (`all_times`)
- Список занятых слотов (`busy_times`)

Нужно:
1. Получить свободные слоты
2. Показать пользователю
3. Дать выбрать время
4. Проверить ввод
5. Записать пользователя

---

##  День 1 — Базовая логика

###  Версия 1: Получение свободных слотов
 Первая задача — просто понять работу списков и фильтрации.

```python
all_times = ["10:00", "11:00", "12:00", "13:00"]
busy_times = ["11:00"]
free_times = []

for time in all_times:
    if time not in busy_times:
        free_times.append(time)

print("Свободные слоты:")
for time in free_times:
    print(time)
```

 **Что я поняла:**
- Как работают списки и `for`
- Как фильтровать данные через `if not in`

---

###  Версия 2: Ввод времени вручную
 Пользователь вводит время сам (например `"10:00"`).

```python
chosen_time = input("Выберите время: ")

while chosen_time not in free_times:
    print("Ошибка, такого времени нет")
    chosen_time = input("Выберите время: ")

print("Вы записаны на:", chosen_time)
```

️ **Проблемы:**
- Неудобно вводить `"10:00"` вручную
- Легко ошибиться в формате
- Не подходит для реального продукта

 **Вывод:** нужно упростить ввод для пользователя.

---

##  День 2 — Интерфейс и валидация

###  Версия 3: Переход на выбор через номера
 Заменила ввод времени на выбор через цифры.

```python
for i in range(len(free_times)):
    number = i + 1
    print(f"{number} - {free_times[i]}")

chosen_number = input("Выберите номер: ")
```

 **Логика:** `номер → индекс → время`

---

###  Версия 4: Первая попытка проверки (ошибочная)
На этом этапе я попыталась сделать проверку сразу в одном условии, но столкнулась с первой серьёзной ошибкой в логике.
 
```python
while not chosen_number.isdigit() and chosen_number not in free_times:
    print("ошибка")
```

 **Проблемы:**
- Логика не работала
- Путаница между `and` и `or`
- Сравнение разных типов (число vs время)

 **Вывод:** нельзя проверять всё сразу.

---

###  Версия 5: Попытка объединить всё в один while
 Я пыталась объединить проверки, чтобы избежать дублирования кода, но это привело к усложнению логики.

```python
while not chosen_number.isdigit() or not 0 < int(chosen_number) <= len(free_times):
    print("ошибка")
```

 **Проблемы:**
- Сложно понять и отладить
- Путаница в логике условий

 **Вывод:** упрощение синтаксиса может усложнить понимание.

---

###  Версия 6: Финальное решение (разделённые проверки)
 Разбила валидацию на два шага.

```python
chosen_number = input("Нажмите цифру, соответствующую времени: ").strip()

# 1. Проверка: это число?
while not chosen_number.isdigit():
    print("Ошибка: введите только цифру.")
    chosen_number = input("Нажмите цифру, соответствующую времени: ").strip()

# 2. Проверка: входит в диапазон?
while not 0 < int(chosen_number) <= len(free_times):
    print("Ошибка: цифры нет в списке.")
    chosen_number = input("Нажмите цифру, соответствующую времени: ").strip()

# 3. Запись
i = int(chosen_number) - 1
chosen_time = free_times[i]
busy_times.append(chosen_time)

print(f"Вы записаны на: {chosen_time}")
```

 **Ключевые выводы:**
- Сначала проверяется тип данных (`isdigit`), потом диапазон
- Нельзя делать `int()` до проверки на цифры
- Лучше разделять логику, чем лепить всё в одну строку
- `.strip()` спасает от случайных пробелов

---

##  Дополнительно
Во время отладки использовала `print(f"DEBUG: '{chosen_number}'")`, чтобы видеть реальные данные (например, скрытые пробелы). Это научило меня доверять выводу, а не только интуиции.

---

##  Дальнейшее развитие проекта

Планирую развивать проект в следующих направлениях:

- добавить возможность подтверждения записи с обработкой различных вариантов ответа (например, повторный выбор)
- реализовать хранение данных (списка записей) между запусками программы
- улучшить структуру кода (разделение на функции)
- протестировать сценарии с различными вариантами пользовательского ввода

В перспективе:

- адаптировать логику под Telegram-бота
- реализовать интерфейс с кнопками вместо текстового ввода

---
 ***Этот проект — не просто код, а процесс обучения: от простой логики → к взаимодействию с пользователем → к защите программы от ошибок.***
 
##  Чему я научилась

- работать с пользовательским вводом
- валидировать данные
- разделять логику программы
- обрабатывать ошибки

---

## День 3 — Логика программы и пользовательский сценарий

### Версия 7: Главный цикл и управление логикой программы

#### 1. Логика программы

На данном этапе была выстроена полная логика взаимодействия с пользователем.

Основой стал цикл `while True`, который управляет всем сценарием работы программы:

- программа не завершается после одного действия  
- пользователь может повторять выбор  
- управление остаётся внутри одного потока выполнения  

В процессе работы я:

- поняла роль `while True` как главного управляющего цикла  
- разобралась, где правильно завершать сценарий с помощью `break`  
- выстроила последовательность действий внутри цикла  
- реализовала повтор сценария при отказе пользователя  

**Логика работы:**  
`вывод → ввод → проверка → подтверждение → действие`

**Цель:**

- реализовать непрерывный пользовательский сценарий  
- сделать поведение программы предсказуемым  
- сохранить контроль над выполнением на каждом этапе  

---

#### 2. Пример кода

```python
all_times = ["10:00", "11:00", "12:00", "13:00"]
busy_times = ["11:00"]
free_times = []

# собираем свободные слоты
for time in all_times:
    if time not in busy_times:
        free_times.append(time)

while True:

    # вывод слотов
    for index in range(len(free_times)):
        number = index + 1
        print(f"{number} - {free_times[index]}")

    # ввод
    selected_number = input("Выберите удобное время (введите номер): ").strip()

    # проверка: число
    while not selected_number.isdigit():
        print("Ошибка: введите цифру.")
        selected_number = input("Выберите удобное время (введите номер): ").strip()

    # проверка: диапазон
    while not 0 < int(selected_number) <= len(free_times):
        print("Ошибка: такого номера нет.")
        selected_number = input("Выберите удобное время (введите номер): ").strip()

    # получаем время
    index = int(selected_number) - 1
    selected_time = free_times[index]

    # подтверждение
    confirmation = input(f"Подтвердите запись на {selected_time} (да/нет): ").strip().lower()

    if confirmation == "да":
        busy_times.append(selected_time)
        print("Вы записаны на:", selected_time)
        break
    else:
        print("Выбор отменён, попробуйте снова.")
```

---

##  Что я проработала в этой версии

#### 1. Работа с текстом (UX)

На этом этапе была проведена работа над текстами, которые видит пользователь:

- упростила формулировки
- убрала перегруженные и неестественные фразы
- сделала сообщения более понятными с первого прочтения
- продумала последовательность взаимодействия

Пример:

 **Было:**  
`"Нажмите цифру соответствующую выбранному времени"`

 **Стало:**  
`"Выберите удобное время (введите номер)"`

---

#### 2. Рефакторинг переменных

Проведена работа по улучшению читаемости кода:

- `i` → `index`
- `chosen_number` → `selected_number`
- `chosen_time` → `selected_time`
- `verification` → `confirmation`

**Цель:**

- сделать код понятным без дополнительного анализа
- убрать неоднозначные названия
- приблизить код к практике реальной разработки

---

#### 3. Структура кода

Код был переработан с точки зрения последовательности выполнения:

- выделены этапы:
  - вывод данных
  - ввод
  - проверка
  - действие

- выстроен логичный порядок операций
- устранены лишние усложнения

**Цель:**

- сделать логику читаемой без запуска программы
- упростить восприятие сценария
- снизить вероятность ошибок при изменении кода

---

##  Итог

В этой версии проект перешёл от набора условий к полноценному пользовательскому сценарию:

✔ реализован полный цикл взаимодействия  
✔ улучшена структура кода  
✔ улучшен пользовательский опыт  
✔ логика стала ближе к реальной практике разработки  

---

##  Вывод

На этом этапе я поняла, что:

- важно не только написать код, но и сделать его понятным
- названия переменных напрямую влияют на читаемость
- `while True` — основа интерактивных программ
- пользователь может передумать, и это нужно учитывать в логике



---

## День 4 — Работа со списками и оптимизация кода

### Версия 8: Оптимизация через list comprehension и обработка крайних случаев

### Знакомство с list comprehension

На этом этапе я продолжила практику на Stepik, решала задачи на работу со списками и условиями.

С инструментом `list comprehension` я столкнулась не напрямую в заданиях, а случайно — увидела решение другого ученика, которое выглядело как одна строка вместо привычного цикла `for`.

Это вызвало интерес, и я начала разбираться:

- как работает такая запись  
- что она делает  
- чем отличается от обычного цикла  

В процессе анализа пришла к пониманию, что:

`list comprehension` — это способ создавать списки более кратко и читаемо по сравнению с классическим циклом.

### Пример: классический подход

```python
numbers = [1, 2, 3, 4, 5, 6]

filtered_numbers = []

for number in numbers:
    if number % 2 == 0:
        filtered_numbers.append(number ** 2)

print(filtered_numbers)
```

---

### Тот же код через list comprehension

```python
numbers = [1, 2, 3, 4, 5, 6]

filtered_numbers = [number ** 2 for number in numbers if number % 2 == 0]

print(filtered_numbers)
```

---

### Вывод

- `list comprehension` сокращает код
- делает его более компактным
- позволяет объединить фильтрацию и преобразование в одной строке

При этом важно учитывать читаемость и не усложнять выражения.

---

## Применение в проекте

После изучения я решила применить этот инструмент в своём проекте.

---

### Было (через цикл)

```python
free_times = []

for time in all_times:
    if time not in busy_times:
        free_times.append(time)
```

---

### Стало (через list comprehension)

```python
free_times = [time for time in all_times if time not in busy_times]
```

---

### Дополнительное улучшение

В процессе работы над логикой программы была выявлена ситуация, когда свободные слоты могут закончиться.

Добавлена проверка:

```python
if not free_times:
    print("Свободных мест нет")
    break
```

---

### Результат

В этой версии:

✔ код стал короче и чище  
✔ улучшена читаемость  
✔ добавлена обработка реальной ситуации (нет свободных мест)  
✔ проект стал более устойчивым  

---

## Итог

На этом этапе я:

- познакомилась с `list comprehension` через анализ чужого решения
- разобралась в его работе
- применила новый инструмент в проекте
- улучшила структуру и устойчивость программы

---

## День 5 — Set, оптимизация и улучшение логики ввода

### Версия 9: Использование set для оптимизации и улучшение валидации ввода

### Неожиданное знакомство с set

На этом этапе я продолжила практику на Stepik и столкнулась с новым для себя инструментом — `set` (множество).

Как и в случае с `list comprehension`, это произошло не напрямую через задание, а случайно: я увидела решение, где использовались фигурные скобки, и это вызвало вопросы.

Я начала разбираться:
- чем `set` отличается от `list`
- как он работает
- в каких ситуациях его использовать

---

### Что я поняла

`set` — это структура данных, которая:
- хранит только уникальные значения
- не сохраняет порядок элементов
- работает быстрее при проверке `"есть элемент или нет"`

Главное отличие от списка:
- `list` → перебирает элементы по порядку
- `set` → выполняет быструю проверку (без перебора)

---

###  Дополнительное открытие: разница между set и dict

В процессе работы я столкнулась с неожиданным моментом: фигурные скобки в Python используются не только для `set`, но и для `dict`.

Ранее я считала, что `{}` — это всегда множество, но оказалось:
- `{}` → это пустой словарь (`dict`)
- `set()` → это пустое множество (`set`)
- `{"кот", "заяц"}` → это множество (`set`)
- `{"кот": 1}` → это словарь (`dict`)

Также на практике проявилось свойство автоматического удаления дубликатов:

```python
a = {"кот", "кот", "заяц"}
print(a)
# Результат: {'кот', 'заяц'}
```
Это помогло мне лучше понять различие структур данных и избежать ошибок при их инициализации.

---

### Применение в задачах

Пример фильтрации с использованием `set` внутри `list comprehension`:
```python
animals = ['слон', 'заяц', 'волк', 'кот']
allowed_animals = {'заяц', 'кот'}

filtered_animals = [animal for animal in animals if animal in allowed_animals]
print(filtered_animals)
```

**Важное понимание:**
- `list comprehension` → создаёт список (результат)
- `set` → используется внутри условия для быстрой проверки
- Итог: `set` ускоряет проверку, `list` хранит итоговую выборку

---

## Применение в проекте (V9)

После изучения `set` появилась идея применить его в проекте для оптимизации.

### Было:
```python
busy_times = ["11:00"]
# добавление: busy_times.append(selected_time)
```

### Стало:
```python
busy_times = {"11:00"}
# добавление: busy_times.add(selected_time)
```

### Почему это лучше:
- быстрее работает проверка `time not in busy_times`
- автоматически исключаются дубликаты (защита от повторной записи)
- логика соответствует задаче: время либо занято, либо нет (порядок не важен)

Обновление свободных слотов осталось прежним, но теперь работает эффективнее за счёт быстрой проверки в `set`:
```python
free_times = [time for time in all_times if time not in busy_times]
```

**Ключевой вывод:**
`list comprehension` → создаёт список  
`set` → ускоряет проверку внутри условия

---

## Исправление логики ввода (ключевое улучшение)

Ранее использовались два отдельных цикла `while`:

```python
while not selected_number.isdigit():
    ...
while not 0 < int(selected_number) <= len(free_times):
    ...
```

### Проблема
После повторного ввода пользователь мог ввести нечисловое значение, и программа падала с ошибкой при попытке выполнить `int(selected_number)` во втором цикле.

### Решение
Оба цикла объединены в один устойчивый блок:

```python
while True:
    selected_number = input("Выберите удобное время (введите номер): ").strip()

    if not selected_number.isdigit():
        print("Ошибка: введите цифру.")
        continue

    if not 0 < int(selected_number) <= len(free_times):
        print("Ошибка: такого номера нет в списке.")
        continue

    break  # Ввод корректен, выходим из цикла валидации
```

### Почему это важно
- каждая новая попытка ввода проверяется с нуля
- полностью исключена ошибка `ValueError`
- логика становится предсказуемой и устойчивой
- код становится чище и понятнее

---

## Итог

На этом этапе я:
- познакомилась с `set` и разобралась в его работе
- поняла разницу между `list`, `set` и `dict` на практике
- научилась комбинировать структуры данных под задачу
- применила `set` в проекте для оптимизации скорости и защиты от дублей
- улучшила обработку пользовательского ввода, сделав её устойчивой к ошибкам

## Вывод

На этом этапе стало понятно:
- важно не только написать код, но и выбрать правильные структуры данных
- разные типы данных могут и должны работать вместе
- `set` отлично подходит для проверки принадлежности, `list` — для хранения результата
- обработка пользовательского ввода — критически важная часть программы, а не "довесок"

Проект стал ближе к реальному приложению с точки зрения логики, скорости и устойчивости.

---


## День 6 — Улучшение подтверждения записи и обработка ошибок

### Версия 10: Confirmation Loop и разделение пользовательских сценариев

### Главная фича версии
На этом этапе проект получил важное улучшение логики взаимодействия с пользователем.

Ранее программа различала только два состояния:

```text
да → подтверждение
всё остальное → отказ
```
Например, ответы `нет`, `banana`, `123`, `привет` обрабатывались одинаково. Для программы это был отказ.

Во время тестирования стало понятно, что это не совсем правильно. **Ошибка ввода и отказ пользователя — разные события.** Если пользователь случайно ввёл `banana`, он не отказывался от записи. Он просто ввёл некорректное значение.

---

### Новая логика подтверждения
В версии V10 подтверждение записи было переработано. Теперь программа различает три состояния:
1. Подтверждение записи
2. Отказ от записи
3. Ошибка ввода

Логика стала выглядеть так:

```text
Получить ответ пользователя
↓
Положительный ответ? → Да → подтвердить запись
↓ Нет
Отрицательный ответ? → Да → отменить выбор времени
↓ Нет
Ошибка ввода → Повторить подтверждение
```

---

### Поддержка нескольких вариантов ответа
Для обработки подтверждения были добавлены группы допустимых ответов:
```python
positive_answers = {'да', 'yes'}
negative_answers = {'нет', 'no'}
```
Для хранения выбран `set`, потому что порядок элементов не имеет значения, дубликаты не нужны, а проверка через `in` работает за O(1).

---

### Нормализация пользовательского ввода
Перед проверкой ответа используется `.strip().lower()`. Это позволяет корректно обрабатывать `ДА`, `Да`, `да`, ` YES`, `yes` и приводить их к единому виду.

---

### Улучшение пользовательского сценария
**Было:** Ошибка ввода → возврат к началу (пользователь терял выбор).  
**Стало:** Ошибка ввода → повтор подтверждения (пользователь остаётся в контексте).

---

### Работа с циклами
К этому моменту уже использовались:
- главный цикл бронирования;
- цикл валидации пользовательского ввода.

В версии V10 был добавлен **третий цикл — отдельный confirmation loop**. Его задача — изолировать логику ответа от выбора времени. Во время работы я лучше поняла:
- как работают вложенные циклы (nested loops)
- что `break` завершает только ближайший цикл
- как использовать результат работы внутреннего цикла после выхода из него

---

### Работа с состоянием программы
Стало понятно, что переменные сохраняют своё значение после выхода из цикла. Это позволило использовать значение `confirmation` после завершения confirmation loop и принимать дальнейшее решение на основе ответа пользователя.

---

### Завершение программы
После успешной записи добавлена команда `exit()`. Теперь программа корректно завершает работу с финальным сообщением, а не просто останавливается.

---

## Итог
На этом этапе я:
- улучшила пользовательский сценарий работы программы
- научилась различать ошибку ввода и отказ пользователя
- реализовала отдельный цикл подтверждения записи
- добавила поддержку нескольких вариантов ответа
- закрепила работу с `set` для проверок
- лучше поняла работу вложенных циклов и `break`
- научилась использовать состояние программы после выхода из цикла

## Вывод
Важно учитывать реальные действия пользователя, а не только писать рабочий код. Ошибка ввода и отказ требуют разной обработки. Циклы — это инструмент реализации логики, а не цель сами по себе. Хорошая программа должна быть удобной не только для разработчика, но и для пользователя.

---

## Дальнейшее развитие проекта
Следующий шаг — сохранение данных между запусками программы:
- сохранять занятые слоты в файл
- загружать данные при запуске
- сохранить состояние записей после закрытия приложения

В перспективе: работа с датами, разделение проекта на функции, адаптация под Telegram-бота.



### 📸 Демонстрация работы программы (V10)

На скриншоте показан полный сценарий работы:
- вывод доступных слотов
- обработка ошибок ввода (буквы, цифры вне диапазона)
- цикл подтверждения (`да`/`нет`/ошибка)
- успешная запись

---

<img width="1547" height="965" alt="image" src="https://github.com/user-attachments/assets/ce82a6e2-fedf-4608-b842-e07540ed7266" />


---

# Day 7 | Booking System V11 | Начало работы с сохранением данных

В предыдущей версии была поставлена задача сохранять данные между запусками программы. Сегодня началась реализация этой функциональности.

До текущего момента все записи существовали только в оперативной памяти. После завершения программы содержимое `busy_times` полностью терялось. Поэтому было принято решение реализовать сохранение занятых слотов в файл.

## Архитектурные решения

Перед написанием кода была продумана логика будущей системы хранения данных:

- Во время работы программы источником правды (Source of Truth) является `busy_times`.
- После завершения программы актуальное состояние сохраняется в файле `busy_times.txt`, из которого данные будут восстановлены при следующем запуске.
- Файл хранит текущее состояние системы, а не историю изменений.
- При изменении данных файл будет полностью перезаписываться.
- Планируется проверять существование файла.
- Если файл отсутствует, программа должна создать его автоматически.
- Если файл существует, данные будут загружаться из него в `busy_times`.

Отдельно было принято решение использовать полную перезапись файла, а не добавление новых данных в конец.

Такой подход позволяет всегда сохранять актуальное состояние системы без накопления устаревших данных.

Например, если слот `11:00` уже содержится в `busy_times`, то при очередном сохранении программа должна записать актуальное состояние множества занятых слотов целиком.

Поэтому для проекта был выбран режим записи `"w"` (write), который полностью обновляет содержимое файла.

Использование режима добавления (`append`, `"a"`) привело бы к накоплению старых данных и усложнило бы восстановление текущего состояния системы.

Также был подробно разобран будущий поток данных:

Загрузка данных:

Файл → str → split("\n") → list → set → busy_times

Сохранение данных:

busy_times → join("\n") → str → файл

## Практическая часть

Сегодня работа велась в отдельной тестовой среде, без внесения изменений в основной код проекта V10.

Были изучены базовые операции работы с файлами:

- функция `open()`;
- режим записи `"w"` (write);
- объект открытого файла;
- метод `write()`;
- метод `close()`.

Был создан первый файл проекта:

`busy_times.txt`

После этого выполнена первая успешная запись данных:

```text
11:00
```
в файл `busy_times.txt`.

### 📸 Первое сохранение данных

<img width="163" height="120" alt="image" src="https://github.com/user-attachments/assets/29326e23-1817-41d5-846c-4e8164934fe9" />

Первая запись в файл: `11:00` успешно сохранена в `busy_times.txt`

## Дополнительный эксперимент

Во время работы над записью данных в файл был проведён небольшой эксперимент с несколькими элементами.

Изначально в файле находилась только одна запись:

```text
11:00
```

Именно этот вариант использовался при первой проверке записи данных в файл на Day 7.

Позже было принято решение протестировать работу символа переноса строки `"\n"` и посмотреть, как будет выглядеть файл при хранении нескольких записей.

Для эксперимента в файл были добавлены дополнительные значения:

```text
11:00
12:00
13:00
```

### 📸 Эксперимент с несколькими записями

<img width="832" height="205" alt="image" src="https://github.com/user-attachments/assets/4a8360c6-48e4-48f6-bf68-6a1fb89d5b37" />

Во время эксперимента также было обнаружено, что символ переноса строки "\n" должен находиться внутри текстовой строки. После исправления данные стали корректно сохраняться на отдельных строках файла.

Данный эксперимент позволил проверить:

- корректную запись нескольких элементов;
- работу символа переноса строки.

Полученный результат подтвердил, что файл может корректно хранить несколько записей на отдельных строках.


## Итоги дня

Сегодня проект впервые вышел за пределы оперативной памяти и получил возможность сохранять данные на диск.

Это первый шаг к реализации постоянного хранения данных (persistent storage), которое позволит Booking System сохранять занятые слоты между запусками программы.

## План на следующий этап

- Научиться читать данные из файла.
- Получать строку (`str`) из содержимого файла.
- Преобразовывать данные в структуру `busy_times`.
- Подготовить систему к интеграции с основным кодом проекта.

 
*Первая практическая цель V11 достигнута: проект успешно создал файл и сохранил данные на диск.*

---

# Day 8 | Booking System V11 | Чтение файла и восстановление состояния системы

Сегодня была завершена основная логика загрузки данных из файла для будущей версии V11.

В предыдущий день программа научилась сохранять данные в файл `busy_times.txt`. Следующей задачей стало восстановление этих данных при запуске приложения.

Главная цель дня — не просто прочитать файл, а вернуть данные в том формате, который ожидает основная логика Booking System.

## Архитектурное решение

Перед написанием кода была подробно разобрана будущая цепочка преобразований данных:

Файл → Текст → Список → Множество → busy_times

Так как файл хранит только текстовые данные, необходимо последовательно преобразовать их обратно в структуру данных проекта.

Именно такую структуру данных использует основная логика проекта для вычисления свободных слотов.


Во время обсуждения были сделаны следующие выводы:

- файл хранит текущее состояние системы;
- файл содержит информацию о занятых слотах;
- свободные слоты вычисляются программой автоматически;
- после загрузки необходимо восстановить именно `busy_times`.

Также была продумана логика запуска программы:

Старт программы → Проверка файла → Чтение данных → Восстановление busy_times → Вычисление свободных слотов → Работа системы

## Чтение файла

Для чтения файла был изучен режим:

```python
open("busy_times.txt", "r")
```

где:

- `r` — read (чтение);
- файл открывается для получения сохранённых данных.

После этого содержимое файла было успешно считано:

```python
file = open("busy_times.txt", "r")

text = file.read()

print(text)
```

Результатом стала строка с содержимым файла:

```text
11:00
12:00
13:00
```

### 📸 Чтение содержимого файла


<img width="387" height="280" alt="image" src="https://github.com/user-attachments/assets/96519688-15e0-4da8-a607-63fa48a67878" />


В этот момент стало понятно, что после чтения файла программа получает обычный текст (`str`).

## Преобразование текста в список

Следующим этапом стало преобразование текста в отдельные элементы.

Так как каждая запись хранится на новой строке, было принято решение использовать разделитель:

```python
"\n"
```

После разделения был получен список:

```python
['11:00', '12:00', '13:00']
```

### 📸 Результат работы split()


<img width="384" height="298" alt="image" src="https://github.com/user-attachments/assets/d79393da-2214-490c-a3a3-cb2dd63c6b7a" />


На этом этапе стало особенно заметно различие между текстом и структурой данных.

Файл хранит один текстовый объект, а программе необходимы отдельные элементы времени, с которыми можно работать дальше.

## Преобразование списка в множество

Так как в проекте `busy_times` хранится в виде множества (`set`), следующим шагом стало преобразование списка в множество.

```python
busy_times = set(times_list)
```

После преобразования был успешно восстановлен набор занятых слотов:

```python
{'11:00', '12:00', '13:00'}
```

### 📸 Восстановление busy_times

<img width="417" height="300" alt="image" src="https://github.com/user-attachments/assets/d65fce4c-d84d-4c9b-b098-977b4cf3783c" />


Во время тестирования также стало заметно, что порядок элементов в множестве может отличаться от порядка в списке. Это подтвердило ранее изученную особенность структуры данных `set`: порядок элементов не гарантируется.

## Личные наблюдения и размышления

Во время работы было интересно сравнить запись и чтение файла.

На первый взгляд чтение выглядит более сложным процессом, так как включает несколько этапов преобразования данных:

Текст → Список → Множество

Однако лично для меня самым сложным этапом оказалась именно запись данных.

Причина была не в сложности кода, а в том, что это было первое знакомство с файловой системой Python.

В начале работы пришлось разобраться сразу с несколькими новыми понятиями:

- что такое файл в программе;
- как работает функция `open()`;
- что означает режим `"w"`;
- как создаётся файл;
- как выполняется запись данных;
- зачем необходимо закрывать файл.

Когда базовые принципы стали понятны, дальнейшая работа пошла значительно легче.

Поэтому чтение файла и последующее преобразование данных уже воспринимались как логичное продолжение ранее изученных шагов.

Также стало понятно, что работа с файлами строится на уже знакомых инструментах Python:

- строки (`str`);
- метод `split()`;
- списки (`list`);
- множества (`set`).

Новой оказалась только сама идея хранения данных вне программы.

## Итоги дня

Сегодня был реализован полный путь восстановления данных из файла:

Файл → Текст → Список → Множество → busy_times

В результате программа научилась получать сохранённые данные и восстанавливать состояние системы после запуска.

Фактически была завершена самая важная часть подготовки V11 — восстановление занятых слотов из постоянного хранилища.

## План на следующий этап

- объединить запись и чтение данных;
- встроить загрузку `busy_times` в основной проект;
- реализовать автоматическое создание файла при первом запуске;
- проверить работу Booking System между несколькими запусками программы.

Вторая практическая цель V11 достигнута: программа успешно считывает данные из файла и восстанавливает состояние системы.


---
