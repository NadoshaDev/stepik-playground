#  AI-Mentor Log #01: Диапазоны, направление и фильтрация
**Дата:** 25.05.2026  
**Файл:** `loops_range/01_conditional_slice.py`  
**Роль ИИ:** Наставник-валидатор логики

###  Что я спросила
- Как корректно работать с `range()` при динамических границах?
- Как учесть направление прохода (вперёд/назад) без дублирования логики?
- Как правильно собрать отфильтрованные элементы и безопасно найти максимум?

###  Где интуиция совпала с подсказкой
Я предполагала, что сначала нужно собрать подходящие элементы в отдельный список, а уже потом работать с ним. ИИ подтвердил: это безопасный паттерн, который разделяет сбор данных и их анализ. Так код остаётся читаемым и предсказуемым.

###  Что я изменила / отклонила и почему
- **Отказалась от отдельного цикла для поиска максимума.** Вместо ручного перебора использовала встроенную `max()`. Это упростило код, убрало лишний проход и сделало намерение явным.
- **Улучшила названия переменных.** `nums`, `start`, `stop`, `filtered_numbers` → чёткие, без абстракций. Код стал читаемым для себя и для будущего меня.
- **Добавила проверку на пустой список.** `if filtered_numbers:` вместо молчаливого `max()` на пустой последовательности. Это не «заплатка», а осознанная граница: программа должна говорить, когда данных недостаточно, а не падать.

 **Вывод:** ИИ не писал за меня. Он подсветил узкие места. Я приняла решение сама. Код стал проще, потому что я убрала лишнее, а не добавила.
